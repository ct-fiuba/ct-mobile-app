import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  actionables: {
    flexDirection: 'row',
    width: '100%',
    margin: 0,
  },
  center: {
    margin: 10,
  },
});

export default styles;
